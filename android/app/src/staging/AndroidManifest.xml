<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application>
        <provider android:name="com.concreteequipmenthire.ceh.BankDocumentProvider"
            android:authorities="${applicationId}.bank_documents"
            android:exported="false" android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/ceh_bank_document_paths" />
        </provider>
        <meta-data
            android:name="com.concreteequipmenthire.ceh.ENVIRONMENT"
            android:value="STAGING" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.update_files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/ceh_update_paths" />
        </provider>
    </application>
</manifest>
